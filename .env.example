# TitleBridge Phase 1B Private Beta — environment template
# Copy to .env.local for local work. Never commit real values.

DATABASE_URL=
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=
GROK_AUTH_ISSUER=
GROK_AUTH_CLIENT_ID=
GROK_AUTH_CLIENT_SECRET=

# Plaid Identity Verification (server-only). Production access is already approved;
# set these in Vercel Production, never in client code.
PLAID_CLIENT_ID=
PLAID_SECRET=
PLAID_ENV=production
PLAID_TEMPLATE_ID=
PLAID_WEBHOOK_SECRET=
